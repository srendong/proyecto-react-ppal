import React from "react";
import "../../assets/styles.css";

const MainSideSubcategory = (props) => {
  return (
    <li>
    <a href="#">{props.name}</a>
  </li>
  );
};

export default MainSideSubcategory;
